# 需求记录

## 临时想法

先把想到的内容写这里，不用整理格式。

- 在部署的电脑上的问题：导入图书数据过于慢 并且导入错误后删除提示 数据库操作失败（p2028）
- 数据库的 bookid是什么来头 我对这个数据库的理解是二维码是每本书唯一的对应 但是在我们这里会出现一些合并的现象
- 项目内许多的文本内容需要删除和调整 一些的前端交互逻辑需要调整
- 需要拥有学生数据的导入 和图书导入同逻辑 需要一样保持兼容性 未来可能需要教师导入
- 校园轮播需要在管理端有删除的选择
- 需要有评语倒出的功能 包含管理员设置的精选的标签
- 需要简易封装一下？ 更加简单的部署流程
- 删除和docker有关的部署相关 不考虑docker
- 管理员需要有更多的权限 比如修改密码 已经密码的修改流程需要更加清晰 之前我记得已经改为了三个管理员了但是现在 只有一份密码的初始化
- 数据库架构可能要修改
- 学号需要兼容格式 3开头 或者202x开头 具体请问我
- 学生ipad需要设置白名单 不一定需要我们改动

## 需求列表

### P0 — 阻塞性问题，影响当前使用

| 编号  | 需求                                           | 状态   | 备注                                         |
| ----- | ---------------------------------------------- | ------ | -------------------------------------------- |
| R-001 | 大文件图书导入过慢；导错批次后删除失败（P2028）| 已完成 | 优化批量写入策略；修复批次删除事务           |
| R-002 | 管理员密码修改流程不清晰；只有一套初始化密码  | 已完成 | 支持各管理员独立改密；初始化只创建缺失账号，不覆盖已有密码  |

### P1 — 核心功能缺失，近期需要

| 编号  | 需求                                                          | 状态   | 备注                                                   |
| ----- | ------------------------------------------------------------- | ------ | ------------------------------------------------------ |
| R-003 | 管理端轮播图支持删除                                          | 已完成 | 支持删除当前轮播图；删除上传副本但保留默认素材源文件；删空后重启不会自动恢复默认轮播 |
| R-004 | 学生数据导入（与图书导入同逻辑，保持列名兼容性）              | 待处理 | 待确认：学号格式（3 开头 or 202x 开头），见待确认问题  |
| R-005 | 评语导出功能（含管理员设置的"精选"标签）                     | 待处理 | 明确导出格式（CSV/Excel）；精选标签的数据模型需设计     |

### P2 — 体验和运维改进

| 编号  | 需求                                                      | 状态   | 备注                                                          |
| ----- | --------------------------------------------------------- | ------ | ------------------------------------------------------------- |
| R-006 | 简化部署流程，去除 Docker 依赖                            | 待处理 | 已有 Windows 无 Docker 方案；评估是否作为主推路径             |
| R-007 | 前端文本内容删减和调整；部分交互逻辑优化                  | 待处理 | 需用户列出具体页面和改动点                                    |

### P3 — 需进一步确认或优先级较低

| 编号  | 需求                                                               | 状态   | 备注                                                         |
| ----- | ------------------------------------------------------------------ | ------ | ------------------------------------------------------------ |
| R-008 | 后端性能优化：SQLite WAL 模式、gzip 压缩、敏感词与首页缓存       | 已完成 | book_id 语义确认无需代码改动；实际并发远低于 200 人，SQLite 优化已足够 |
| R-009 | 教师数据导入                                                       | 待处理 | 未来需求，暂不排期；实现时与学生导入复用同一套框架           |
| R-010 | 学生 iPad 白名单访问控制                                           | 待确认 | 大概率为网络/路由器侧配置，不涉及代码改动；待运维侧确认      |

## 待确认问题

- [ ] R-004：学号格式兼容规则 — 是否同时兼容「3 开头（短格式）」和「202x 开头（长格式）」？两种格式如何区分或映射？
- [ ] R-005：评语导出格式 — 导出 CSV 还是 Excel？「精选标签」是给每条评语打标、还是单独维护一个精选列表？

## 变更记录

| 日期       | 内容                                 |
| ---------- | ------------------------------------ |
| 2026-04-13 | 创建需求记录                         |
| 2026-04-13 | 整理临时想法，补充 R-002 至 R-010    |
| 2026-04-13 | 切换至 r001 分支，R-001 标记已完成   |
| 2026-04-16 | R-003 标记已完成；管理端轮播图支持删除 |
| 2026-04-16 | R-008 标记已完成；后端性能优化（WAL、压缩、缓存） |
| 2026-04-17 | 整理仓库根目录：数据文件归入 data/，补全 backend/ 和 admin-frontend/ .gitignore |
